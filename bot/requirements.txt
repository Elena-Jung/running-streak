# 재현 가능한 빌드를 위해 버전 고정(검증된 조합). 업그레이드는 의도적으로.
# [voice]: 음악 기능용 PyNaCl + davey(DAVE E2EE — 2026-03 부터 디스코드 음성에 필수).
discord.py[voice]==2.7.1
davey==0.1.6
asyncpg==0.30.0
pytesseract==0.3.13
Pillow==12.2.0

# 유튜브 추출(음악 기능). [default]=JS 챌린지 해결 포함 권장 의존성, [deno]=JS 런타임 동봉.
# 유튜브 변경으로 추출이 깨지면 이 핀을 최신으로 올려 재빌드하는 것이 1순위 대응.
yt-dlp[default,deno]==2026.7.4

# 개발/테스트 전용(런타임 불필요하나 이미지에 포함해 컨테이너서 pytest 실행).
pytest==9.1.1
